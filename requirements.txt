# Updated requirements - pinned to secure, compatible versions
# Python: 3.14+ (detected)
# NOTE: On some platforms (Windows) compiled packages (numpy/scipy) may not have
# prebuilt wheels for Python 3.14. Pip may try to build them from source which
# requires a C/C++ toolchain. If installation fails, consider using Python 3.11/3.12
# or a conda/miniforge environment.
numpy==2.3.5
pandas==2.3.3
matplotlib==3.10.8
requests==2.32.5
pyyaml==6.0.3
scipy==1.16.3
regex==2025.11.3
tqdm==4.67.1
lxml==6.0.2
typing_extensions==4.15.0
